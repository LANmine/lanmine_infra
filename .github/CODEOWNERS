# Changes to automation, deploy scripts, and CI config require review from a
# maintainer (they run with repo secrets, so a bad change here is high-impact).
# Apprentice changes under stacks/ are NOT affected by this file.

/.github/       @hermanl0 @chhaugen
/scripts/       @hermanl0 @chhaugen

# Network device configs push to real switches over the mgmt VLAN from the
# self-hosted runner — high impact, maintainer review required.
/network/       @hermanl0 @chhaugen
